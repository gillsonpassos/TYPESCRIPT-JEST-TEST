describe('TESTANDO ALGUMA COISA', () => {
  it('should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('TESTANDO OUTRA COISA', () => {
  test('should return Molusco', () => {
    const name = 'Molusco';
    expect(name).toBe('Molusco');
  });
});
